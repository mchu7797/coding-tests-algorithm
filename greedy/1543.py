document = input()
keyword = input()

index = 0
keyword_found = 0
document_end = len(document)
keyword_size = len(keyword)

while index < end:
    if document[index] == keyword[0]:
        for i in range(index, index + keyword_size):
            
    index += 1